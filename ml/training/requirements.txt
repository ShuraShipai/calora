# Local, open-source dependencies only. Installation is intentionally manual.
torch>=2.2,<3.0
torchvision>=0.17,<1.0
Pillow>=10.0,<12.0
PyYAML>=6.0,<7.0
numpy>=1.26,<3.0
scikit-learn>=1.4,<2.0

# Add a detector framework only after the multi-food annotation set is ready.
# Example candidate (optional): ultralytics>=8.0,<9.0
